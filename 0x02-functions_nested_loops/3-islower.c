#include "main.h"
#include <stdio.h>
/**
 * _islower - checks the lowercase
 * @c: integer
 * Return: void
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
return (0);
}
