#include "main.h"
#include <stdio.h>
/**
 * _isdigit - check for a digit
 * @c: this s the integer
 * Return: 0 (sucess)
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
		return (1);
	else
		return (0);
return (0);
}
